{
  "compilerOptions": {
    "declaration": true,
    "target": "ESNext",
    "module": "ESNext",
    "baseUrl": ".",
    // 是否跳过声明文件的类型检查，这可以在编译期间以牺牲类型系统准确性为代价来节省时间，默认：false
    "skipLibCheck": true,
    // 是否跳过默认库声明文件的类型检查，默认：false
    "skipDefaultLibCheck": true,
    // strictNullChecks”:严格检查空值
    "strictNullChecks": true,
    // 不允许函数参数双向协变
    "strictFunctionTypes": true,
    // 类的实例属性必须初始化
    "strictPropertyInitialization": true,
    "useUnknownInCatchVariables": false,
    "noImplicitThis": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "moduleResolution": "node",
    // 通过导入内容创建命名空间，实现CommonJS和ES模块之间的互操作性
    // 开启改选项时，也自动开启- allowSyntheticDefaultImports 选项
    "esModuleInterop": true,
    // 是否允许从没有默认导出的模块中默认导入，默认：false
    // "allowSyntheticDefaultImports": true,
    // 是否启用对装饰器的实验性支持，装饰器是一种语言特性，还没有完全被 JavaScript 规范批准，默认：false
    // "experimentalDecorators": true,
    // 为装饰器启用对发出类型元数据的实验性支持，默认：false
    // "emitDecoratorMetadata": false
    // https://tsup.egoist.dev/#troubleshooting
  },
  "include": ["**/*.ts"]
}
